# AGENTS.md — G:\moneythings-goal 项目规则

本文件是当前工作区（项目文件夹）的强制工作规则，任何代码修改、任务执行都必须遵守。

## 1. 代码提交到 GitHub（强制）

- 每次写完代码（任何修改），必须立即提交到 GitHub，不允许堆积多次修改后才一次性提交。
- 提交流程：
  1. `git add` 暂存本次改动的文件
  2. 写清晰、有意义的 commit message（说明改了什么、为什么改）
  3. `git commit`
  4. `git push` 推送到 GitHub 远程仓库
- 若当前目录还不是 git 仓库，先初始化 git 并关联 GitHub 远程仓库再开始工作。

## 2. 详细更新报告（强制）

- 每次完成任务或一次修改后，必须向用户提供详细的更新报告，包含：
  - 本次改动了哪些文件（完整路径）
  - 每个文件改了什么、为什么改
  - 如何验证 / 测试结果
  - 对应的 commit hash 与提交信息
  - GitHub 推送状态（已 push / 未 push 及原因）

## 3. 工作日志（强制）

- 每次工作会话结束后，必须追加写入工作日志。
- 日志文件：项目文件夹内的 `worklog.md`（不存在则创建）。
- 每条日志至少包含：
  - 日期时间
  - 本次任务内容
  - 修改的文件列表
  - commit hash
  - 遇到的问题与解决方案（如有）
  - 下一步计划（如有）

## 4. 文件修改范围限制（强制）

- 所有文件修改、创建、删除仅限于当前项目文件夹：`G:\moneythings-goal`。
- 禁止修改、删除、移动项目文件夹之外的任何文件（包括但不限于：其他项目、系统目录、用户主目录下的无关文件、全局配置文件等）。
- 项目文件夹之外的内容只在任务需要时读取，且只读不写。

## 5. 规则冲突处理

- 本文件规则优先于全局 AGENTS.md；两者冲突时，以更严格的规则为准。
